# Contributing to Snack Meet

All development happens in the single Snack Meet repository. Do not add nested Git repositories, upstream remotes, or update manifests belonging to another product.

Before submitting a change, run the same frontend and Rust checks defined in the root CI workflow. Preserve meeting data compatibility and include recovery behavior for every file/database mutation.
